[package]
name = "esexpr-utils"
version = "0.1.0"
edition = "2024"
rust-version = "1.96.0"

[dependencies]
esexpr-text = { workspace = true }
esexpr-binary = { workspace = true, features = ["std"] }

embedded-io-adapters = { workspace = true, features = ["std"] }
